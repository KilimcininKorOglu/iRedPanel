{
    "name": "kilimcininkoroglu/iredpanel",
    "version": "1.1.0",
    "description": "Lightweight PHP admin panel for managing iRedMail users and domains via LDAP or MySQL",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.1",
        "vlucas/phpdotenv": "^5.6",
        "phpmailer/phpmailer": "^7.1"
    },
    "suggest": {
        "ext-ldap": "Required for LDAP backend and for Active Directory replication",
        "ext-pdo": "Required for MySQL and PostgreSQL backends",
        "ext-pdo_mysql": "Required for MySQL backend",
        "ext-pdo_pgsql": "Required for PostgreSQL backend",
        "ext-sodium": "Required for Active Directory replication (encrypted bind password)"
    },
    "config": {
        "platform": {
            "php": "8.1.0"
        }
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5 || ^11.5 || ^12.5 || ^13.0"
    }
}
